# 复制为 .env 后按需修改（.env 已被 .gitignore 忽略，不要提交真实凭据）
DATABASE_URL=postgres://<用户名>:<密码>@127.0.0.1:5432/redstation
PORT=3001
HOST=0.0.0.0
# 生产环境务必替换为随机长字符串
JWT_SECRET=change-me-to-a-random-secret
JWT_EXPIRES_IN=7d
DB_POOL_MAX=10
